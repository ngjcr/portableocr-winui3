# OCR runtime placeholder

The source-only bundle intentionally omits the ~155 MB OCR runtime. Run `Import-Runtime.ps1` from the repository root against an extracted copy of the original PortableOCR Studio folder, or copy `resources/app/engines` and `resources/app/licenses` into this `runtime` directory.
